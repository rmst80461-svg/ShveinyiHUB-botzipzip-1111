 В боте реализована админка, но текущая реализация неудобна для управления большим количеством заказов - отсутствует пагинация, фильтрация и удобная навигация.

Текущая реализация (из предоставленных файлов):
1. orders.py - основной файл обработки заказов:
Создание заказов через ConversationHandler

Уведомление администраторов о новых заказах через notify_admins()

Функция get_admin_order_keyboard() для управления конкретным заказом

Изменение статусов через handle_order_status_change()

Отправка уведомлений клиентам при изменении статуса

2. messages.py - обработка сообщений и callback-запросов:
Функция handle_admin_callback() для обработки административных действий

Показывает только текстовые сообщения без пагинации

Есть кнопки: "📊 Статистика", "📦 Управление заказами"

3. handlers.py - дополнительные хендлеры:
show_admin_stats() - показывает статистику

show_spam_candidates() - показывает кандидатов на спам

4. keyboards.py - клавиатуры:
get_admin_orders_submenu() - подменю выбора категорий заказов

get_admin_order_detail_keyboard() - клавиатура для детального просмотра заказа

get_admin_main_menu() - главное меню админа (Reply Keyboard)

Проблемы текущей реализации:
Нет пагинации - все заказы показываются списком, при 10+ заказах неудобно читать

Нет удобной навигации между заказами

Нет фильтрации кроме выбора статуса

Нет поиска по номеру заказа или клиенту

Нет массовых действий с заказами

Задание для AI Replit:
Создать улучшенную систему управления заказами в админке с:

1. Система пагинации с фильтрацией
Отображение заказов по 8-10 на странице

Кнопки навигации "◀️ Назад", "Вперед ▶️"

Индикатор текущей страницы (Страница 1/5)

Фильтры по статусам: 🆕 Новые, 🔄 В работе, ✅ Готовые, 📤 Выданные

2. Удобный просмотр заказа:
Компактное отображение в списке: номер, имя, статус, дата

Детальный просмотр по клику

Быстрое изменение статуса без ухода со страницы

3. Поиск и фильтрация:
Поиск по номеру заказа

Поиск по имени клиента

Фильтрация по дате

4. Быстрые действия:
Смена статуса в один клик

Массовые действия (выбрать несколько заказов)

Связь с клиентом прямо из админки

5. Архитектурные требования:
Не ломать существующую логику создания заказов

Использовать существующие функции из utils.database:

get_orders_by_status() - получение заказов по статусу

update_order_status() - обновление статуса

get_order() - получение информации о заказе

Использовать существующие callback-паттерны

Сохранить уведомления администраторов и клиентов

Технические детали реализации:
Новые callback_data форматы:
text
orders_list_{status}_{page}          # Список заказов с пагинацией
order_detail_{order_id}_{page}       # Детали заказа
order_status_{order_id}_{new_status} # Изменение статуса
order_search_{type}                  # Поиск заказа
order_filter_{filter_type}_{value}   # Фильтрация
Пример структуры сообщения со списком заказов:
text
📦 Список заказов (🆕 Новые)

📋 Заказ #24-12.25-#15
👤 Иван Иванов | 📞 +7 999 123 45 67
📅 24.12.2024 14:30 | 🆕 Новый

📋 Заказ #24-12.25-#14
👤 Мария Петрова | 📞 Telegram
📅 24.12.2024 13:45 | 🔄 В работе

Страница 1 из 5
Новые функции для реализации в admin_handlers.py:
show_orders_list() - отображение списка с пагинацией

show_order_detail() - детальный просмотр заказа

handle_order_search() - обработка поиска

handle_bulk_actions() - массовые действия

create_orders_keyboard() - генерация клавиатуры для списка

Доработка keyboards.py:
get_orders_list_keyboard() - клавиатура для списка с пагинацией

get_order_actions_keyboard() - действия с конкретным заказом

get_search_keyboard() - клавиатура поиска

get_filters_keyboard() - фильтры

Пример кода для вдохновления:
python
async def show_orders_list(update: Update, context: ContextTypes.DEFAULT_TYPE, status="new", page=0):
    """Показать список заказов с пагинацией"""
    query = update.callback_query
    await query.answer()
    
    # Получаем заказы по статусу
    orders = get_orders_by_status(status)
    
    # Пагинация
    per_page = 8
    start_idx = page * per_page
    end_idx = start_idx + per_page
    current_orders = orders[start_idx:end_idx]
    total_pages = (len(orders) + per_page - 1) // per_page
    
    # Формируем сообщение
    text = f"📦 Список заказов ({STATUS_EMOJI[status]} {STATUS_NAMES[status]})\n\n"
    
    for order in current_orders:
        formatted_id = format_order_id(order.id, order.created_at)
        text += f"📋 Заказ {formatted_id}\n"
        text += f"👤 {order.client_name} | 📞 {order.client_phone}\n"
        text += f"📅 {order.created_at.strftime('%d.%m.%Y %H:%M')} | {STATUS_EMOJI[order.status]} {STATUS_NAMES[order.status]}\n\n"
    
    text += f"Страница {page+1} из {total_pages}"
    
    # Создаем клавиатуру
    keyboard = []
    
    # Кнопки заказов
    for order in current_orders:
        formatted_id = format_order_id(order.id, order.created_at)
        keyboard.append([
            InlineKeyboardButton(
                f"{formatted_id} - {order.client_name}",
                callback_data=f"order_detail_{order.id}_{page}"
            )
        ])
    
    # Кнопки навигации
    nav_buttons = []
    if page > 0:
        nav_buttons.append(
            InlineKeyboardButton("◀️ Назад", callback_data=f"orders_list_{status}_{page-1}")
        )
    nav_buttons.append(
        InlineKeyboardButton(f"{page+1}/{total_pages}", callback_data=f"orders_page_info")
    )
    if page < total_pages - 1:
        nav_buttons.append(
            InlineKeyboardButton("Вперед ▶️", callback_data=f"orders_list_{status}_{page+1}")
        )
    
    keyboard.append(nav_buttons)
    
    # Кнопки фильтров
    filter_buttons = [
        InlineKeyboardButton("🆕 Новые", callback_data=f"orders_list_new_0"),
        InlineKeyboardButton("🔄 В работе", callback_data=f"orders_list_in_progress_0"),
        InlineKeyboardButton("✅ Готовые", callback_data=f"orders_list_completed_0"),
        InlineKeyboardButton("📤 Выданные", callback_data=f"orders_list_issued_0"),
    ]
    keyboard.append(filter_buttons)
    
    # Кнопки действий
    action_buttons = [
        InlineKeyboardButton("🔍 Поиск", callback_data="order_search"),
        InlineKeyboardButton("📊 Статистика", callback_data="admin_stats"),
        InlineKeyboardButton("◀️ В админку", callback_data="admin_back_menu"),
    ]
    keyboard.append(action_buttons)
    
    await query.edit_message_text(
        text=text,
        reply_markup=InlineKeyboardMarkup(keyboard),
        parse_mode="Markdown"
    )
Что должно быть в финальном решении:
Новый файл admin_orders.py с улучшенной логикой

Обновленные клавиатуры в keyboards.py

Интеграция с существующими хендлерами

Сохранение всех существующих функций (уведомления, создание заказов)

Плавная навигация между заказами

Минимальные изменения в существующем коде

Цель:
Создать интуитивно понятный интерфейс администратора, который позволяет эффективно управлять 50+ заказами без необходимости прокрутки длинных списков, с быстрым доступом к нужным функциям и информативным отображением данных.